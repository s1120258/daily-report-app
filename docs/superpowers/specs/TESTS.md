# 営業日報システム テスト仕様書

## 方針

| 種別 | ツール | 対象 |
| --- | --- | --- |
| 単体テスト | Vitest | ユーティリティ・純粋関数（`lib/utils.ts`, `lib/session.ts`） |
| 統合テスト（API） | Jest | Route Handlers の権限制御・ビジネスロジック・DB連携 |
| E2Eテスト | Playwright | 主要ユーザーフローのブラウザ操作 |

Vitest はテスト DB 不要。Jest は `.env.test` の専用 PostgreSQL を使用。

## テストデータ

統合テストは `.env.test` で管理する専用 PostgreSQL に接続。各テストケース前後にテーブルをクリーンアップ。

**ユーザー fixture**

| ID | ロール | メール |
| --- | --- | --- |
| 1 | manager | manager@test.com |
| 2 | sales | sales-a@test.com |
| 3 | sales | sales-b@test.com |

**顧客 fixture**: ID=10（株式会社テストA）、ID=11（株式会社テストB）

## 単体テスト（Vitest）

P 列: H=高優先 / M=中 / L=低

### `lib/utils.ts`

| TC | テストケース | 期待結果 | P |
| --- | --- | --- | --- |
| UT-01 | `serializeId(BigInt(123))` | `"123"` | M |
| UT-02 | `formatDate(new Date("2026-05-17T10:00:00Z"))` | `"2026-05-17"` | M |

### `lib/session.ts`

| TC | テストケース | 期待結果 | P |
| --- | --- | --- | --- |
| UT-03 | セッションなしで `getSessionUser()` を呼ぶ | `null` を返す | H |
| UT-04 | セッションありで `getSessionUser()` を呼ぶ | `{ id, role }` オブジェクトを返す | H |

---

## 統合テスト（API）

P 列: H=高優先 / M=中 / L=低

### 認証共通

| TC | テストケース | 前提 | 期待結果 | P |
| --- | --- | --- | --- | --- |
| AUTH-01 | 未認証でAPIにアクセス | セッションなし | `401` | H |
| AUTH-02 | 有効なセッションでAPIにアクセス | salesセッション | `200` / `201` | H |

### `GET /api/daily-reports`

| TC | テストケース | 前提 | 期待結果 | P |
| --- | --- | --- | --- | --- |
| DR-L-01 | sales が自分の日報一覧を取得 | 営業Aで2件・営業Bで1件 | 営業Aの2件のみ返る | M |
| DR-L-02 | manager が全員の日報一覧を取得 | 計3件 | 3件すべて返る | M |
| DR-L-03 | manager が `user_id` で絞り込み | 全員分あり | 指定営業の日報のみ返る | M |
| DR-L-04 | `from` `to` で期間絞り込み | 複数日の日報あり | 期間内のみ返る | M |
| DR-L-05 | sales が `user_id` を指定 | 他人の `user_id` | 自分の日報のみ返る（パラメータ無視） | M |

### `POST /api/daily-reports`

| TC | テストケース | 前提 | 期待結果 | P |
| --- | --- | --- | --- | --- |
| DR-C-01 | sales が正常に日報を作成 | 当日の日報なし | `201`・DBにレコード作成 | H |
| DR-C-02 | 訪問記録なしで作成 | `visitRecords: []` | `201`・日報のみ作成 | H |
| DR-C-03 | 訪問記録を複数行つけて作成 | 3件 | `201`・visit_records 3件作成 | H |
| DR-C-04 | 同一日の日報を2回作成 | 当日分が既に存在 | `409` | H |
| DR-C-05 | `reportDate` を省略 | 必須項目欠落 | `400` | H |
| DR-C-06 | 存在しない `customerId` を指定 | `customerId: 9999` | `400` | H |
| DR-C-07 | manager が作成しようとする | managerセッション | `403` | H |

### `GET /api/daily-reports/[id]`

| TC | テストケース | 前提 | 期待結果 | P |
| --- | --- | --- | --- | --- |
| DR-R-01 | sales が自分の日報を取得 | 自分の日報 | `200`・詳細・訪問記録返る | H |
| DR-R-02 | sales が他人の日報を取得 | 他人の日報ID | `403` | H |
| DR-R-03 | manager が任意の日報を取得 | 営業Aの日報ID | `200` | H |
| DR-R-04 | コメント済みの日報を取得 | problem_comment あり | `problemComment` にコメント内容 | H |
| DR-R-05 | コメントなしの日報を取得 | コメントなし | `problemComment: null`・`planComment: null` | H |
| DR-R-06 | 存在しない ID を指定 | `id: 9999` | `404` | H |

### `PUT /api/daily-reports/[id]`

| TC | テストケース | 前提 | 期待結果 | P |
| --- | --- | --- | --- | --- |
| DR-U-01 | sales が自分の日報を更新 | 自分の日報 | `200`・DB更新 | M |
| DR-U-02 | 訪問記録を追加して更新 | 既存1件 → 2件に | `200`・2件になる | M |
| DR-U-03 | 訪問記録を削除して更新 | 既存2件 → `[]` | `200`・0件になる | M |
| DR-U-04 | 訪問記録を部分差し替え | 既存2件の1件削除・1件追加 | `200`・送信内容に洗い替え | M |
| DR-U-05 | sales が他人の日報を更新 | 他人の日報ID | `403` | H |
| DR-U-06 | manager が更新しようとする | managerセッション | `403` | H |

### `PATCH /api/daily-reports/[id]/problem-comment`

| TC | テストケース | 前提 | 期待結果 | P |
| --- | --- | --- | --- | --- |
| DR-PC-01 | manager が Problem にコメントを追加 | コメントなし | `200`・内容・コメント者・日時返る | M |
| DR-PC-02 | manager が既存コメントを上書き | コメントあり | `200`・上書きされる | M |
| DR-PC-03 | sales がコメントしようとする | salesセッション | `403` | H |
| DR-PC-04 | 存在しない日報IDにコメント | `id: 9999` | `404` | H |

### `PATCH /api/daily-reports/[id]/plan-comment`

| TC | テストケース | 前提 | 期待結果 | P |
| --- | --- | --- | --- | --- |
| DR-PLC-01 | manager が Plan にコメントを追加 | コメントなし | `200` | M |
| DR-PLC-02 | manager が既存コメントを上書き | コメントあり | `200`・上書きされる | M |
| DR-PLC-03 | sales がコメントしようとする | salesセッション | `403` | H |

### `GET /api/customers` / `POST /api/customers` / `PUT /api/customers/[id]`

| TC | テストケース | 前提 | 期待結果 | P |
| --- | --- | --- | --- | --- |
| CU-L-01 | sales が顧客一覧を取得 | salesセッション | `200` | L |
| CU-L-02 | manager が顧客一覧を取得 | managerセッション | `200` | L |
| CU-C-01 | manager が顧客を登録 | 必須項目あり | `201`・DB作成 | L |
| CU-C-02 | `name` を省略 | 必須項目欠落 | `400` | L |
| CU-C-03 | sales が登録しようとする | salesセッション | `403` | M |
| CU-C-04 | 任意項目を省略して登録 | phone・email なし | `201`・null で作成 | L |
| CU-U-01 | manager が顧客を更新 | 既存顧客 | `200`・DB更新 | L |
| CU-U-02 | sales が更新しようとする | salesセッション | `403` | M |
| CU-U-03 | 存在しない ID を指定 | `id: 9999` | `404` | L |

### `GET /api/users` / `POST /api/users` / `PUT /api/users/[id]`

| TC | テストケース | 前提 | 期待結果 | P |
| --- | --- | --- | --- | --- |
| US-L-01 | manager がユーザー一覧を取得 | managerセッション | `200` | L |
| US-L-02 | sales がアクセス | salesセッション | `403` | H |
| US-C-01 | manager が sales ユーザーを登録 | 必須項目あり | `201`・DB作成 | L |
| US-C-02 | manager が manager ユーザーを登録 | `role: "manager"` | `201` | L |
| US-C-03 | 既存メールアドレスで登録 | 同一メール存在 | `409` | L |
| US-C-04 | `email` を省略 | 必須項目欠落 | `400` | L |
| US-C-05 | sales が登録しようとする | salesセッション | `403` | M |
| US-U-01 | manager がユーザーを更新 | 既存ユーザー | `200`・DB更新 | L |
| US-U-02 | パスワードを空文字で更新 | `password: ""` | `200`・パスワード変更なし | L |
| US-U-03 | パスワードを変更して更新 | `password: "newpass"` | `200`・更新される | L |
| US-U-04 | sales が更新しようとする | salesセッション | `403` | M |
| US-U-05 | 存在しない ID を指定 | `id: 9999` | `404` | L |

## E2Eテスト（Playwright）

### 認証フロー

| TC | テストケース | 手順 | 期待結果 | P |
| --- | --- | --- | --- | --- |
| E2E-AUTH-01 | 正常ログイン（sales） | メール・パスワードを入力してログイン | 日報一覧に遷移 | H |
| E2E-AUTH-02 | 正常ログイン（manager） | manager でログイン | 担当者フィルターが表示される | H |
| E2E-AUTH-03 | 誤ったパスワードでログイン | 不正パスワードを入力 | エラーメッセージが表示される | H |
| E2E-AUTH-04 | ログアウト | ログアウトを実行 | ログイン画面に遷移 | H |
| E2E-AUTH-05 | 未認証で直接URLアクセス | `/` に直接アクセス | ログイン画面にリダイレクト | H |

### 日報作成フロー（sales）

| TC | テストケース | 手順 | 期待結果 | P |
| --- | --- | --- | --- | --- |
| E2E-DR-01 | 日報を新規作成 | 新規作成 → 訪問記録1件 + Problem/Plan 入力 → 保存 | 詳細画面に遷移し入力内容が表示される | H |
| E2E-DR-02 | 訪問記録を複数行追加 | 行追加ボタンで3行追加して保存 | 3件の訪問記録が表示される | H |
| E2E-DR-03 | 訪問記録を削除してから保存 | 2行追加 → 1行削除 → 保存 | 1件の訪問記録が表示される | M |
| E2E-DR-04 | 日報を編集 | 詳細から「編集」→ 内容変更 → 保存 | 変更後の内容が反映される | M |
| E2E-DR-05 | 同一日に2回作成しようとする | 既存日報がある状態で「新規作成」 | エラー表示または新規作成ボタンが非表示 | M |

### コメントフロー（manager）

| TC | テストケース | 手順 | 期待結果 | P |
| --- | --- | --- | --- | --- |
| E2E-CM-01 | Problem にコメントを追加 | 詳細を開く → Problem コメント欄に入力 → 保存 | コメント内容・者名・日時が表示される | M |
| E2E-CM-02 | Plan にコメントを追加 | 詳細を開く → Plan コメント欄に入力 → 保存 | コメント内容が表示される | M |
| E2E-CM-03 | コメントを上書き保存 | 既存コメントを変更して保存 | 変更後のコメントが表示される | M |
| E2E-CM-04 | sales の画面でコメント欄が非表示 | sales でログイン → 日報詳細を確認 | コメント入力欄が表示されない | M |

### 顧客マスタ管理フロー（manager）

| TC | テストケース | 手順 | 期待結果 | P |
| --- | --- | --- | --- | --- |
| E2E-CU-01 | 顧客を新規登録 | 顧客マスタ → 新規登録 → 保存 | 一覧に追加される | L |
| E2E-CU-02 | 顧客情報を編集 | 一覧から「編集」→ 変更 → 保存 | 変更後の名前が反映される | L |
| E2E-CU-03 | sales に登録ボタンが非表示 | sales でログイン → 顧客マスタ一覧を確認 | 新規登録・編集ボタンが非表示 | L |

### ユーザー管理フロー（manager）

| TC | テストケース | 手順 | 期待結果 | P |
| --- | --- | --- | --- | --- |
| E2E-US-01 | ユーザーを新規登録 | 管理一覧 → 新規登録 → 保存 | 一覧に追加される | L |
| E2E-US-02 | ユーザー情報を編集 | 一覧から「編集」→ 変更 → 保存 | 変更後の名前が反映される | L |
| E2E-US-03 | パスワードを空白のまま更新 | 編集画面でパスワードを空欄にして保存 | 既存パスワードでログインできる | L |
| E2E-US-04 | sales がユーザー管理画面にアクセス | URL 直打ち | 403 またはリダイレクト | L |
